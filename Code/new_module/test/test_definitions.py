import pytest
from . import Timescale
#from timecalculus.timescale import Timescale
#from definitions import sigma, rho, mu, nu, integers, quantum
from fractions import Fraction
"""
Timescale2 = integers(a=0, b=20)
print(Timescale2.ts)
# Emplear función decoradora.

Timescale1=Timescale([0,Fraction(1,3),Fraction(1,2),Fraction(7,9),1,2,3,4,5,6,7],'documentation example')

for indice in range(len(Timescale1.ts)):
	print("sigma({}) es {}.".format(indice, sigma(Timescale1, indice)))


for indice in range(len(Timescale1.ts)):
	print("rho({}) es {}.".format(indice, rho(Timescale1, indice)))


# TODO: Corregir
#for indice in range(len(Timescale2.ts)):
#	print("mu({}) es {}.".format(indice, mu(Timescale2, indice)))

#print(mu(Timescale2, 1))

Timescale3 = quantum(q=3, m=0, n=10)
print(Timescale3.ts)

#for indice in range(len(Timescale1.ts)):
#	print("nu({}) es {}.".format(indice, nu(Timescale1, indice)))
"""